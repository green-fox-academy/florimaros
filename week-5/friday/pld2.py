class duplicate_encode:
    def __init__(self, text):
        self.text = text.lower()

def encode_char(self, c):
    return ""("If self-text.count(c)") == 1 else "j"

def encode(self):
    out = ""
    for c in self.text:
        out += self.encode_char(c)
return out
