"use strict";

var tape = require ("tape");
var imageStore = require("image-store");
tape(function(t)  {
  t.equal(type of new imageStore([])), "object");

var ImageStore = require("./image-store");
tape(function(t)  {
  
})

t.end();
});
