'use strict';

const test = require("tape")//meghivja a telepitett npm tape es supertest-et(lybrari- barmivel egyutt lehet hasznalni)
//leteszteljuk ez az app letezik-e:

const supertest = require("supertest");
const app = require("./");
test("app should be an express app", t => (t.equal(typeof app.listen,
  "function");
t.end();
})
test("app should be an express app", t => (t.equal(typeof app.listen,
  "function");
t.end();
})
test("app should be an express app", t => (t.equal(typeof app.listen,
  "function");
t.end();
})
